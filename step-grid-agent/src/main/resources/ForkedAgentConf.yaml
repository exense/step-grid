---
# This is the default configuration of the forked agent
registrationPeriod: 1000
gridReadTimeout: 20000
workingDir: ./work
ssl: false
tokenGroups:
- capacity: 1
  tokenConf:
    attributes:
      forkedAgentId: ${forkedAgentId}
agentForker:
  enabled: false